<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Online Auction</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item dropdown">
            <% if session[:userdata].nil? %>
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Session
          </a>
        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%# <li><%@category=Category.all %
                <%# <select name="category" style="vertical-align:top;">
                    <option value="Category">Category</option>
                    <% @category.each do |cat| %>
                    <%# <option value="<%= cat.name %>
                    <%# "><%= cat.name %>
                    <%# </option> %>
                    <%# <% end %>
                <%# </select>
            </li>--> %> 


                    <div class="nav-link dropdown-item"><a href="/login">Login</a></div>
                    <div class="nav-link dropdown-item"><a href="/register">Register</a></div>
                <% else %>
                    <li class="nav-link text-nowrap"><%= session[:userdata]["name"] %></li>
                    <li><a class="nav-link text-nowrap" href="/profile/">Your Profile</a></li>
                    <li><a class="nav-link text-nowrap" href="/product/">Your Products</a></li>
                    <li><a class="nav-link text-nowrap" href="/logout">Log Out</a></li>
                <% end %>
        </li>
          </ul>
      </ul>
     <div class="float-right">
        <form class="d-flex float-right" action="/search" method="GET">

            <input type="text" class="d-flex float-right" name="searchterm" placeholder="Search">
            <input type="submit" class="btn btn-outline-primary" value="Search"/>
        </form>
    </div>
    </div>
    
  </div>
</nav>
